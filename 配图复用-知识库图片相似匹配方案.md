# 配图复用 —— 基于知识库内容的图片相似匹配 实施方案

> 状态：已评审细化，待实施
> 目标项目：OpenBidKit_Yibiao（Electron 桌面端）
> 关联需求：让"配图"这一块能复用历史标书里的图，而不是每次都从零生成

---

## 1. 背景与问题

当前系统里，图的知识链路是"断的"，存在两个缺口：

1. **知识库把图丢了**。文档解析时走 `preserveImages: false`，图片在转 Markdown 阶段被 `stripMarkdownImages` 正则剥掉、物理丢弃。知识库只存文字（`title/resume/content`），历史标书里的流程图、结构图、进度图无法沉淀。
2. **配图阶段只造新图、不复用旧图**。正文生成后，`contentIllustrationPlanning` + `contentIllustrationGeneration` 只做"规划 → AI/Mermaid/HTML 重新生成"，从不去知识库查"我们以前有没有类似的图"。

**要解决的核心问题**：历史标书里已经有很多质量不错、经过实战检验的图，能否把它们按"内容相似度"匹配出来，在新标书里复用，而不是每次都重新生成？

---

## 2. 目标与非目标

### 目标

- 知识库解析时**提取图片**，连同图注、上下文文字、类型标签一起建成"图条目"，可检索、可溯源。
- 配图阶段新增"**先查历史相似图 → 命中则复用 → 未命中才生成**"的流程。
- 匹配以**语义相似**为主（图注 + 上下文 + 类型标签），**不做像素级视觉相似**作为主路。

### 非目标（本期不做）

- 不做 Chinese-CLIP 多模态 embedding 作为主检索（成本高、对标书图表类效果存疑，仅作后续可选兜底）。
- 不改动正文生成阶段的核心逻辑（text 条目的召回链路保持原样）。
- 不做跨项目/多租户的图库（本地单机范围）。

---

## 3. 现状代码锚点

下面这些位置是本方案改动的落点，均已核对过实际代码。

### 知识库解析（图在这里被丢）

- `client/electron/services/knowledgeBaseService.cjs`
  - `prepareDocument`（L1165）：知识库文档索引流水线。
  - L1219：`parseDocumentWithConfig(..., { assetScope: 'knowledge-...', preserveImages: false })` ← **图片在此被丢弃**。
  - `createFinalItems`（L999）：把候选条目 + 块匹配合并成最终条目落库，`content` = 源块原文拼接。
- `client/electron/services/fileService.cjs`
  - `parseDocumentWithConfig`（L521）：按 `preserveImages` 分支。
  - `stripMarkdownImages`（L333）：剥掉图片的现成函数。
  - `rewriteMarkdownImages`（L490）、`resolveImageToAssetUrl`（L435）、`createAssetContext`（L340）、`saveImportedImage`（L371）：**现成的"抽图落盘"能力**，支持 data URL / 远程 / zip / 本地四种来源。

### 知识库存储与召回

- `client/electron/services/knowledgeBaseStore.cjs`
  - `readItems`（L1034）、`readReferences`（L1060）：从 `knowledge_items` / `knowledge_item_blocks` 读条目。
- `client/electron/services/contentGenerationTask.cjs`
  - `loadContentKnowledgeReferences`（L2100）：把 `readReferences` 结果拆成 `knowledgeItems`（id+title+resume）和 `knowledgeContentMap`（id→content）。
  - `buildChapterContentPlanMessages`（L793）：正文编排，LLM 从轻量条目选 `knowledge.item_ids`。
  - `resolveKnowledgeContents`（L2136）：按选中的 id 取正文。
  - `buildChapterContentMessages`（L888）：把选中正文作为"参考正文素材"注入生成。

### 配图阶段

- `client/electron/services/contentIllustrationPlanning.cjs`
  - `buildIllustrationPlanningContext`（L46）、`buildIllustrationPlanningPrompt`（L143）。
  - 三类图配置（L99–L118）：`ai` / `mermaid` / `html`，各有 `allowed_types` 和 `image_type` 分类体系。
- `client/electron/services/contentIllustrationGeneration.cjs`
  - `generateAiIllustration`（L240）、`generateMermaidIllustration`（L266）、`generateHtmlIllustration`（L442）。
  - `applyGeneratedIllustrationsToDocument`（L488）、`buildGeneratedIllustrationMarkdown`（L450）。

### 数据库 Schema

- `sql/workspace_schema.sql`：`knowledge_items`、`knowledge_item_blocks`、`knowledge_blocks` 等表；头部注释记录了版本演进（当前 `knowledge_* v3`）。

---

## 4. 总体设计

### 4.1 核心思路：图条目化（Image-as-Item）

**把"图"当成知识库的一等公民，用和文字条目完全一样的结构存进去**，从而复用现有的"目录式召回"。

关键判断：标书里的图，它的语义几乎都写在**图注 + 上下文文字**里。因此匹配主路走"文字 → 图"的语义召回，而不是"图 → 图"的像素相似。

### 4.2 数据模型

在 `knowledge_items` 表上**加三列**（SQLite 加法迁移，向下兼容）：

```sql
ALTER TABLE knowledge_items ADD COLUMN item_kind TEXT NOT NULL DEFAULT 'text';  -- 'text' | 'image'
ALTER TABLE knowledge_items ADD COLUMN image_type TEXT;                          -- 复用配图规划的 image_type 分类
ALTER TABLE knowledge_items ADD COLUMN asset_url  TEXT;                          -- 图本体本地 URL（仅 image 条目）
```

图条目示例（沿用现有 `createFinalItems` 的字段名）：

```js
{
  id: 'K000012',                              // 沿用现有 id 规则
  title: '核心业务上线实施进度网络图',            // 图注
  resume: '描述某项目上线实施阶段的进度安排…',      // LLM 生成的上下文摘要
  content: '![图注](yibiao-asset://imported-images/<batch>/image-0001.png)',  // 图引用
  item_kind: 'image',
  image_type: '进度网络图',
  source_block_ids: ['blk-0042'],              // 溯源到原文块
  source_file: 'xxx.md',
}
```

> 说明：`content` 对图条目而言是"图引用"（Markdown 图片语法或 asset URL），对文字条目仍是"原文"。这样 `resolveKnowledgeContents` 无需改逻辑即可把图也"取出来"。

### 4.3 匹配机制分层

| 层 | 手段 | 作用 | 优先级 |
|---|---|---|---|
| L0 去重 | pHash / dHash | 索引时避免同一张图重复入库 | 必做；Phase 3 实施，此前允许重复入库 |
| L1 类型收窄 | `image_type` 标签 | 把候选从"全库"缩小到"同类图" | 主路第一关 |
| L2 语义匹配 | LLM 目录式召回（图注 + 上下文摘要 vs 当前小节） | 选出真正语义相似的图 | 主路第二关 |
| L3 兜底（可选） | Chinese-CLIP / Mermaid 结构匹配 | 图注缺失、纯示意图、流程图结构 | 后续增强 |

---

## 5. 分阶段实施步骤（可执行）

### Phase 0 —— 图条目化（最小闭环，先能"存"和"看"）

**目标**：知识库解析时不再丢图，把图建成条目；在知识库页面能看到图条目。

**改动文件**：

1. `client/electron/services/fileService.cjs`
   - 不改现有函数，新增导出一个小工具 `extractImagesFromMarkdown(markdown, assets, context)`，复用 `rewriteMarkdownImages` + `resolveImageToAssetUrl`，返回 `[{ asset_url, alt, blockRef }]`。

2. `client/electron/services/knowledgeBaseService.cjs`
   - `prepareDocument` 的 L1219 调用，把 `preserveImages: false` 改为 `true`（并用独立 `assetScope: 'knowledge-<documentId>'`，与查重隔离）。
   - 新增一个索引步骤 `extract_image_items`（可参照现有 `extract_first_items` 的步骤化写法，复用 `runDocumentStep` 落盘缓存）：
     1. 从带图的 Markdown 里定位每张图 + 它的图注（`![alt](...)` 的 alt，或紧邻的 `*图：xxx*`）+ 它所在的块（前后 N 字上下文）。
     2. 用一次 LLM 调用对每张图产出 `{ title, resume, image_type }`（image_type 从配图规划的 `allowed_types` 词汇表里选）。
     3. 组装图条目，写入 `knowledge_items`（`item_kind='image'`、`content=图引用`、`asset_url`、`source_block_ids`）。

3. `sql/workspace_schema.sql`
   - 在 `knowledge_items` 建表语句里补上三列；头部版本注释 `knowledge_* v3` → `v4`。

4. `client/electron/services/knowledgeBaseStore.cjs`
   - `readItems` / `readReferences` 的 SELECT 加上 `item_kind, image_type, asset_url`，随条目一起返回。

5. IPC 与 Renderer 展示链路
   - 无需新增 IPC 通道：`knowledge-base:read-items`（`knowledgeBaseIpc.cjs` L17）已把 `readItems` 结果原样返回，Store 加列后自动带上新字段。
   - `client/electron/preload.cjs` 与 `client/src/shared/types/ipc.ts`：同步条目类型（`item_kind/image_type/asset_url`）。
   - `client/src/features/knowledge-base/`：条目列表/详情按 `item_kind === 'image'` 渲染（缩略图走 `asset_url` + 图注 + 类型标签），点开复用现有图片预览能力。

**验收标准**：
- 上传一份含图的 Word/PDF 到知识库，解析完成后 `knowledge_items` 里出现 `item_kind='image'` 的条目。
- 知识库页面能看到该图条目（图注 + 缩略图）。
- 图能点开，`source_block_ids` 能定位回原文块。
- 存量（未重新解析的）文档不受影响。

---

### Phase 1 —— 召回分流（text / image 分开走）

**目标**：图条目不注入正文生成，而是单独暴露给配图阶段。

**改动文件**：

1. `client/electron/services/contentGenerationTask.cjs`
   - `loadContentKnowledgeReferences`（L2100）：按 `item_kind` 拆分返回：
     - `knowledgeItems` / `knowledgeContentMap`：只含 `text` 条目（正文生成链路保持原样）。
     - 新增 `imageItems`：含 `image` 条目的 `{ id, title, resume, image_type, asset_url, source_file }`。
   - 调用处（L3433 附近）：`imageItems` 只留在任务上下文（Main 内存），**不落库、不经 Renderer 中转**。所选知识库文档 id 已持久化在技术方案状态（`storedPlan.referenceKnowledgeDocumentIds`，contentGenerationTask.cjs L2094/L2999），任务恢复（resume 到 illustration 阶段）时重新调用 `loadContentKnowledgeReferences` 即可重建 `imageItems`。

2. `client/electron/services/knowledgeBaseStore.cjs` 的 `search`（L256）——全局检索对图条目的约定：
   - 图条目的 `content` 是图引用 URL 语法，不参与子串匹配；图条目仅按 `title` / `resume` 参与匹配。
   - 返回行补 `item_kind`、`image_type`、`asset_url`，知识库页面按类型区分展示；同步检查 `knowledgeBaseService.search` 的结果组装。

**验收标准**：
- 正文生成时，图条目**不会**作为"参考正文素材"注入 `buildChapterContentMessages`。
- `imageItems` 能被配图阶段读到（含 resume 重建场景）。
- 全局检索中图条目仅按 title/resume 匹配，content 的 URL 语法不参与匹配；页面按类型区分展示。

---

### Phase 2 —— 配图复用预检（核心价值）

**目标**：配图规划前先查历史相似图，命中就复用，未命中才生成。

**改动文件**：

1. 新增 `client/electron/services/illustrationReuseService.cjs`（或并入现有配图服务），实现：
   - `retrieveSimilarIllustrations({ planItems, sections, imageItems, aiService })`：
     1. **L1 收窄**：按 plan item 的 `image_type` 过滤 `imageItems`（精确匹配 + 一个小型同义词映射，如"进度网络图"≈"里程碑计划图"）。
     2. **L2 语义匹配（每个 plan item 一次 LLM 调用）**：把"当前小节正文 + 该配的图类型"和 L1 收窄后的候选图条目列表（title+resume）打包进同一次调用，由 LLM 选出 top-1，返回 `{ reuse_item_id, confidence }` 或 `{ reuse: false }`；不做逐候选调用。
     3. 命中且 `confidence ≥ 阈值`（默认 0.8）的，给 plan item 打上 `reuse_source = { item_id, asset_url, confidence }`。
   - **安全校验**（复用前必做，规则先行 + LLM 兜底）：先用规则关键词表（客户名、报价数字、logo、地址/地点等）扫图注与上下文，命中即降级为"仅参考，重新生成"；规则未命中时，LLM 在语义匹配中附带输出"是否疑似含项目专有信息"，疑似同样降级。任何不确定情形默认禁止复用（宁可重新生成，不冒带入旧项目信息的风险）。

2. `client/electron/services/contentIllustrationPlanning.cjs`
   - 在 `buildIllustrationPlanningPrompt` 产出 plan 之后，调用 `retrieveSimilarIllustrations`，把 `reuse_source` 写回 plan item。

3. `client/electron/services/contentIllustrationGeneration.cjs`
   - 生成循环里：plan item 若有 `reuse_source`，**跳过生成**，直接把 `asset_url` 用作 `generation.asset_url`，图注走现有 `buildGeneratedIllustrationMarkdown` 逻辑（可选：让 LLM 仅改写图注，不碰图内文字）。

> 本期不限制同一张历史图在新标书内被多个小节/标段重复命中复用；单份标书内去重留待后续增强。

**验收标准**：
- 造一个"历史图 + 新标书同类型章节"的场景：配图规划能召回历史图并标记 `reuse_source`，生成阶段直接用旧图，不调用生图模型。
- 无相似图时，行为与现状一致（走 AI/Mermaid/HTML 生成）。
- 命中图带旧项目名/报价时，被安全校验拦截，走"重新生成"。
- 复用的图随正文正常导出 Word（`exportService.cjs` 已支持 `yibiao-asset://` 协议，验证链路即可）。

---

### Phase 3 —— 增强（其中 L0 pHash 去重必做，其余按需）

- **pHash/dHash 去重（必做）**：索引时对 `asset_url` 算 dHash，重复图不入库（避免历史标书之间大量重复图）。未落地前允许同一张图重复入库。
- **Mermaid 结构匹配**：若历史图是 mermaid 代码，用"节点集合 + 边集合"的 Jaccard/图编辑距离做结构相似。
- **Chinese-CLIP 兜底**：仅对"无图注、无上下文"的孤立图启用视觉检索，不进入主路。

---

## 6. 数据迁移

- `knowledge_items` 加列是加法迁移，老数据 `item_kind` 默认 `'text'`，安全。
- **存量文档的图已永久丢失**（当时 `preserveImages:false` 剥掉了），无法事后补建。需要用户对历史文档**重新上传/重新解析**，才能建图条目。这点要在产品上提示。
- 迁移脚本建议仿照现有 `knowledge_migration_meta` 的做法，加一个 `schema_version` 检查，`ALTER TABLE` 幂等执行。

---

## 7. 测试与验收

### 单元 / 集成

1. `extractImagesFromMarkdown`：四种来源（data URL / 远程 / zip / 本地相对路径）都能抽图落盘并返回 asset_url。
2. `createFinalItems` 图条目分支：title/resume/image_type/content/asset_url 字段正确落库。
3. `loadContentKnowledgeReferences`：text 与 image 正确分离，正文生成拿不到 image 条目。
4. `retrieveSimilarIllustrations`：同类型候选能命中，不同类型不串味；confidence 阈值生效。
5. `knowledgeBaseStore.search`：图条目仅按 title/resume 匹配，content 中的 URL 语法不参与匹配。

### 端到端场景

- **场景 A（命中复用）**：知识库有历史"进度网络图" → 新标书同类章节配图 → 复用旧图，日志显示未调用生图模型。
- **场景 B（未命中生成）**：无历史图 → 走现有生成流程，行为不变。
- **场景 C（安全拦截）**：历史图图注含旧客户名 → 拦截，走重新生成。
- **场景 D（回归）**：纯文字知识库的正文生成流程，改后与改前结果一致（图不干扰正文）。

---

## 8. 风险与边界

| 风险 | 说明 | 应对 |
|---|---|---|
| 存量图丢失 | 老文档图已剥掉，无法补 | 产品提示重新解析；或接受"只对新文档生效" |
| 复用旧图带入旧项目信息 | 图内文字可能含客户名/报价/logo | 复用前安全校验 + 图注改写，命中风险词则重新生成 |
| LLM 匹配成本 | 每次复用预检要调 LLM | 先用 image_type 收窄候选，控制 LLM 调用次数 |
| 图注缺失/不准 | 影响语义匹配质量 | 用上下文块补 resume；极端情况走 L3 视觉兜底 |
| 版权/敏感 | 历史图可能含第三方 logo | 复用仅限"自有历史标书"，脱敏后使用 |

---

## 9. 未来增强

- 图条目的**版本/使用计数**：记录每张历史图被复用了多少次、在哪些项目用过，逐步沉淀"公司级图库"。
- 图注/图内文字自动改写：复用旧图时，用 LLM 自动替换图内项目专有名词，做到"结构复用、信息换新"。
- 与配图规划打通反向沉淀：新生成的图，生成后自动回写为图条目，形成"生成 → 入库 → 下次复用"的正循环。
- 单份标书内复用去重：同一历史图被多个小节/标段命中时，控制重复次数或按小节上下文微调图注。
