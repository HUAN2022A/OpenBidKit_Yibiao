# 开发交接文档（2026-09-08）

> 本文件记录 2026-09-08 的 UI 改造批次与最终 git 状态，供后续接手者快速对齐。功能批次见 [开发交接-2026-09-07.md](开发交接-2026-09-07.md)。

## 一、本次完成的改动（分两条线）

### 1. 核心功能改动（已进 main）

在 09-07 功能基础上补的一批业务改动，已通过 commit `1d5eb07` 提交到 `main` 并推送：

| 功能 | 核心改动 |
|---|---|
| **打赏弹窗可关闭** | `donation:dismiss-prompts` 通道 + `dismissPrompts()`；`DonationPromptProvider` 新增「不再提醒」按钮；`DONATION_PROMPT_ENABLED = false` 全局开关默认关闭，`promptDismissed` 持久化 |
| **导出格式增强** | 页眉新增公司 logo（内置 `header-logo.jpg`）、右栏（`header_right_text`，支持 `{project_name}` 占位）、下划线；导出默认值向真实标书靠拢（边距 2.2cm、行距 1.5、表框黑、页码 `{page}`）；新增「正式投标格式」预设 |
| **模板 Word 识别护栏** | `templateWordAnalyzer.cjs` 新增出厂默认值护栏：忽略 Word/WPS 出厂纸张（Letter/Legal）、出厂边距（1/1.25 英寸）、出厂字号（五号及更小），避免覆盖中文标书默认值 |

### 2. UI 改造（feat/ui-redesign 分支，未合并）

一次从「换色」到「品牌重塑 + 布局重构」的完整探索，最终落定在独立分支 `feat/ui-redesign`（commit `e850cd7`），**未合并进 main**：

| 维度 | 内容 |
|---|---|
| **改名** | 易标 → **铸标 / ForgeBid**（含 Electron 窗口标题、preload `appName`、打包 `productName`） |
| **新 logo** | 图形标记：深灰黑 `#2e2d33` 圆角方块 + 工业红 `#c40000` 菱形 + 白色向上箭头（「投标命中标的」意象）；生成 7 档 PNG + `icon.ico` + `icon.icns` |
| **拟态风格** | 现代化 Neumorphism：浅灰基底 `#e8ebf0` + 卡片 `#eef1f5`；柔和双阴影（凸起）+ `--yb-shadow-inset`（凹陷）；圆角收窄到 6/10/14/20px；工业红做交互强调；深色侧边栏 `#2e2d33` |
| **流程向导式布局** | 信息架构从「功能域分组」改为「流程阶段分组」：工作台首页（默认入口）+ 准备/生成/审核/导出；新增顶栏（全局搜索 + 设置） |
| **隐藏入口** | 移除插件管理、文档、加群 |

## 二、git 分支与 PR 状态（重要）

| 分支 | commit | 内容 | 状态 |
|---|---|---|---|
| `main` | `1d5eb07` | 核心功能 + **旧 UI「易标」** | 已推送，本地/远程一致 |
| `feat/ui-redesign` | `e850cd7` | 新 UI「铸标」（拟态 + 流程布局 + 新 logo） | 已推送，对应 open [PR #3](https://github.com/HUAN2022A/OpenBidKit_Yibiao/pull/3)，**不合并** |

- 新 UI 只作为 PR 分支保留，核心功能不变。
- 曾有一次 UI 合并（PR #2）后被 force-push 回退，PR #2 在 GitHub 上仍显示「已合并」的历史记录；当前有效 UI PR 是 **#3**。
- 仓库 remote：`origin` = HUAN2022A/OpenBidKit_Yibiao（推送目标），`upstream` = FB208/OpenBidKit_Yibiao。`gh` 默认仓库仍指向 FB208，用 gh 操作需显式 `--repo HUAN2022A/OpenBidKit_Yibiao`。

## 三、UI 改造技术细节（供合并/继续迭代时参考）

- **token**（`client/src/styles/tokens.css`）：主色 `--yb-primary:#c40000`、`--yb-violet:#8f1310`（深红）、`--yb-bg:#e8ebf0`、`--yb-surface:#eef1f5`；阴影为拟态双阴影（`--yb-shadow*`）+ `--yb-shadow-inset`。
- **菜单重组**（`client/src/app/menuConfig.ts` + `client/src/shared/types/navigation.ts`）：`SectionId` 移除旧功能域分组（`bid-generation/template-settings/knowledge-base/bid-check`），新增 `home/prepare/generate/review/export`；子项按流程阶段归组。
- **新增文件**：`client/src/features/workbench/pages/WorkbenchHome.tsx`、`client/src/styles/feature-workbench.css`、`client/assets/icon.icns`。
- **顶栏**：`client/src/components/AppShell.tsx` 内新增 `app-topbar`（搜索框为纯 UI 占位，未接真实搜索逻辑）。
- **logo 生成**：PIL 脚本绘制（无 SVG 渲染依赖），多尺寸由 `icon_256.png` 缩放。
- **同步点**：新增菜单页面需同步 5 处（navigation/menuConfig/AppRouter/Sidebar/analytics traffic.js），已在 `traffic.js` 加入 `home/prepare/generate/review/export` 中文名。

## 四、验证方式速查

- `cd client && npm run dev` 启动；`npm run build` 类型检查 + 构建。
- main 分支：启动后为旧 UI「易标」+ 新功能（打赏「不再提醒」、导出页眉 logo/右栏、模板识别护栏）。
- 切到 `feat/ui-redesign` 分支：启动后为新 UI「铸标」，重点看工作台首页（默认落地页）、侧边栏流程分组（底部只剩设置）、顶栏搜索框凹陷拟态。

## 五、遗留问题与待办

1. **顶栏搜索框未接真实逻辑**：跨功能检索模板/素材/知识库需要新的 IPC 通道。
2. **工作区垃圾文件待清理**：`client/_test_electron*.cjs`、`client/.tmp-opencode-download/`、`client/vendor/opencode/`、`header-logo.jpg`（已在 main）、3 份 `.md` 方案文档（部分已提交）。
3. **主进程换行符/stat 噪音**：`core.autocrlf=true` 下 sed 重写导致部分文件出现「幽灵 modified」（内容无实质变化），`git reset --hard` 后可清除。
4. **UI 分支若要合并**：先解决 PR #3 的冲突/审阅，合并后需重跑 `npm run build` + Electron 窗口验证。

## 六、下一步建议

- 若决定采用新 UI：合并 PR #3，随后清理旧品牌文案残留、补顶栏搜索、逐 feature 页面打磨拟态细节。
- 若维持旧 UI：删除 `feat/ui-redesign` 分支与 PR #3，保持 main 现状。
- 无论哪种，建议先清理第五节第 2 条的工作区垃圾文件。
