# 开发交接文档（2026-09-07）

> 本文件记录当天开发批次的功能、当前状态、验证方式与遗留问题，供后续接手者快速对齐。

## 一、本次完成的功能

当天在 `client/`（Electron 桌面端）完成了一批功能与修复，均为未发布状态：

| # | 功能 | 核心改动 |
|---|---|---|
| 1 | **配图复用**：知识库图片不再丢弃，可被后续标书复用 | `knowledge_items` 加 `item_kind/image_type/asset_url` 三列（migration v24）；知识库解析 `preserveImages: true` + `extract_image_items` 步骤；配图规划前查历史相似图命中则复用（`illustrationReuseService.cjs`），未命中才生成；`reuse_source` 持久化（migration v25）；复用图复制进生成目录与知识库生命周期解耦 |
| 2 | **全局离线模式**：一键关闭所有运营类出网请求 | 配置新增 `offline_mode`（默认 false）；抑制公告/升级/广告/埋点/IP封禁检查/模型信息/许可证刷新/Agent诊断上传/资源/插件市场/捐赠等出网点；设置页新增「网络 → 离线模式」开关 |
| 3 | **图片知识库**：独立页面聚合所有历史图片 | `knowledge-base:list-image-items` 通道 + `ImageKnowledgeBasePage`（缩略图/类型筛选/关键词/分页/大图预览/查看原文跳转定位源文档）；侧边栏「图片知识库」占位改为真实页面 |
| 4 | **模板 Word 识别**：上传模板 Word 自动配置导出参数 | `templateWordAnalyzer.cjs`（docx 解析 + 规则映射 + 表格/图片样式解析；.doc/.wps 复用 `withLegacyWordDocxFile` 多后端转换）；`templates:analyze-word` 通道；新建模板页「上传 Word 模板识别」入口 + 识别摘要面板 + 预填确认 |
| 5 | 修复：知识库文件夹重命名无反应 | 根因 `window.prompt` 在 Electron 不支持；改为 `AppDialog` 输入弹窗 |
| 6 | 修复：技术方案「重置」按钮无反应 | 根因 `window.confirm` 不支持；改为 `AppDialog` 确认弹窗 |

## 二、方案文档（仓库根目录）

- `配图复用-知识库图片相似匹配方案.md`
- `全局离线模式方案.md`
- `模板Word识别方案.md`
- `CLAUDE.md`（代码库给 Claude Code 的指引，含命令/架构/硬性要求）

## 三、当前状态

- 开发时使用 `npm run dev`（Vite `127.0.0.1:5173` + Electron）联调；`npm run build`、`npm run smoke:electron-native`、`node --test` 均通过。
- 数据库 schema 已升至 **v25**（v24 知识库图片三列、v25 `reuse_source_json`），`sql/workspace_schema.sql` 已同步。
- 依赖 `better-sqlite3` 是 Electron 原生模块，普通 `node --test` 会 ABI 失败，需 `ELECTRON_RUN_AS_NODE=1 npx electron --test`。
- 本机验证环境：有 WPS（无 COM 注册）、有 Microsoft Word COM 后端（`.doc → .docx` 转换实测可用）、无 LibreOffice。

## 四、验证方式速查

| 功能 | 验证步骤 |
|---|---|
| 配图复用 | 上传含图文档到文档知识库 → 解析出「图片条目」→ 技术方案配图阶段观察是否复用历史图（未命中才生成）→ 暂停/继续验证复用标记不丢 → 删除知识库文档后确认标书图片完好 |
| 离线模式 | 设置 → 网络 → 离线模式开启 → 重启确认公告/升级/广告不弹、资源/插件页显示离线占位 |
| 图片知识库 | 侧边栏「知识库 → 图片知识库」→ 查看聚合图片、筛选/搜索/翻页、查看原文跳转 |
| 模板识别 | 模板设置 → 新建模板 → 上传 Word 模板识别 → 选 .doc/.docx → 看摘要与预填 |
| 重命名/重置 | 知识库文件夹「重命名」弹输入框；技术方案「重置」弹确认框 |

## 五、遗留问题（均不阻塞，按优先级）

1. **模板识别未接 AI 兜底**：未知字体/字号等无法归类项当前直接置默认值（用户已确认本期不做 AI 兜底）。
2. **复用图生命周期**：已通过"复制进生成目录"解决删除知识库文档导致的破图；但复制失败时回退重新生成，属安全行为。
3. **主进程离线守卫入口不统一**：各服务内联判断 `offline_mode`，仅 updateService 用 `isOfflineModeEnabled` 助手；功能等价，属重构建议。
4. **纵深防御缺口**：`updateService.getLatestVersion/getDownloadUrl`、`donationService` 主进程侧无离线守卫（当前仅用户主动入口触发）。
5. **`analytics.ts` 从 `remoteNotice.ts` 导入 `loadOfflineModeEnabled`**：埋点模块依赖公告模块，建议抽到共享工具。
6. **`listImageItems` 无专属单测**（现有测试覆盖 search 钳制与图条目规则）。
7. **工作区垃圾文件待清理**：`client/_test_electron*.cjs`、`client/.tmp-opencode-download/`、`client/vendor/opencode/`（本次未提交，建议确认后删除或加 .gitignore）。

## 六、下一步建议

- 用户在 UI 端到端验收四个功能（重点：模板识别用真实 .doc、配图复用全链路）。
- 若功能验收通过，建议按功能拆分为多个 PR 提交到 upstream（FB208/OpenBidKit_Yibiao）。
- 清理第七点的工作区垃圾文件。
- 模板识别后续可选项：openxmlhelper（.NET）作为第二解析源、.doc/.wps 自动转 docx。
