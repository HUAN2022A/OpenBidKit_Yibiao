# 易标投标工具箱 · 阶段性开发进展汇报

> **汇报周期**：2026-09-07 ~ 2026-09-08
> **项目**：OpenBidKit_Yibiao（开源 AI 标书编写桌面工具，Electron）
> **仓库**：HUAN2022A/OpenBidKit_Yibiao（已合并 `main`）

---

## 一、总体进展（结论先行）

**一句话总结**：本阶段为易标交付 **4 个新能力** + **2 项缺陷修复**，全部完成端到端验证并已合并 `main`，累计改动 **59 个文件、+5513 行**，数据库 schema 升级至 **v25**，24 个单元/集成用例全部通过。

| 关键维度 | 结果 |
|---|---|
| 🎯 交付功能 | 4 个新能力 + 2 项修复 |
| 📦 代码规模 | 59 个文件，+5513 / -96 行 |
| ✅ 质量验证 | build / 语法检查 / 24 个单测 / smoke 全绿 |
| 🗄️ 数据库 | schema v23 → **v25**（图条目、复用来源标注） |
| 🚀 发布 | 已通过 PR #1 合并进 `main` |

### 成果清单

| # | 能力 / 修复 | 解决的核心问题 | 状态 |
|---|---|---|---|
| 1 | **配图复用** | 历史标书图片不再被丢弃，新标书**优先复用**旧图（类型无关召回 + 复用结果可追溯），降低重复生图成本 | ✅ 完成 |
| 2 | **全局离线模式** | 一键关闭所有运营类出网请求，实现数据完全本地化 | ✅ 完成 |
| 3 | **图片知识库** | 独立页面聚合所有历史图片，支持检索/筛选/溯源 | ✅ 完成 |
| 4 | **模板 Word 识别** | 上传模板 Word 自动解析出导出参数，告别 1650 行手动表单 | ✅ 完成 |
| 5 | 修复：知识库文件夹重命名 | 原 `window.prompt` 在 Electron 下无效导致点击无反应 | ✅ 修复 |
| 6 | 修复：技术方案重置按钮 | 原 `window.confirm` 无效导致按钮静默失效 | ✅ 修复 |

---

## 二、分项进展

### 1. 配图复用（知识库图片相似匹配）

**问题**：知识库解析文档时图片被物理丢弃，配图阶段只会重新生成、从不复用历史图。

**方案**：图条目化 + 分层召回——
- 解析时保留图片，连同图注/上下文/类型标签建成与文字条目同构的"图条目"；
- 配图时先查历史相似图（L1 类型收窄 → L2 语义匹配），命中且安全校验通过则复用，未命中才生成；
- 复用图复制进生成目录，与知识库文件生命周期解耦。

**交付**：数据库迁移 v24/v25、知识库 `extract_image_items` 索引步骤、`illustrationReuseService.cjs` 复用服务、生成阶段复用短路。

**增强（09-08）**：
- 新增「优先复用知识库图片」开关（`preferKnowledgeImageReuse`，默认开启）：召回从「按类型收窄」升级为「类型无关关键词召回 + 排序兜底」，解决 HTML 类配图与知识库类型词汇不对齐导致召回失效的问题；
- 复用命中回填来源标题/类型，新增「查看配图结果」弹窗，可追溯每个配图位置的复用/生成状态与把握度；
- 全量重新生成前新增覆盖确认弹窗；「仅重新配图」从开发者面板移入普通用户入口。

**验证**：单元/集成测试通过；覆盖命中复用、未命中生成、安全拦截、正文回归四类场景；新增 `illustrationReuseService.test.cjs`（跨词汇匹配、零重叠兜底、相关性排序等 4 用例）。

### 2. 全局离线模式

**问题**：应用启动会主动发起多类出网请求（公告/升级/广告/埋点/许可证等），用户希望数据完全本地化、不泄露。

**方案**：配置开关 `offline_mode`（默认关闭，一键开启），分层抑制全部运营类出网；用户自配的 AI 模型、MinerU 解析等服务不受影响。

**交付**：设置页"离线模式"开关 + 12 个出网点守卫 + 资源/插件/捐赠页离线占位提示。

### 3. 图片知识库

**问题**：侧边栏"图片知识库"是占位入口，历史图片散落在文档知识库内。

**方案**：同源展示——新增聚合查询，把 `item_kind='image'` 的图条目跨文档捞出，做成独立网格浏览页。

**交付**：`listImageItems` 聚合查询 + IPC 全链路 + `ImageKnowledgeBasePage`（缩略图/类型筛选/关键词检索/分页/大图预览/查看原文跳转定位源文档）。

### 4. 模板 Word 识别

**问题**：模板设置需手动逐项配置 1650 行表单，用户手里已有现成的公司模板 Word。

**方案**：上传 docx（.doc/.wps）→ 解析排版事实 → **规则映射**为导出参数（确定性、零模型、离线可用）→ 预填表单人工确认；表格/图片样式一并解析。

**交付**：`templateWordAnalyzer.cjs`（docx 解析 + 规则映射 + 表格/图片样式）、`templates:analyze-word` 通道、新建模板页"上传 Word 模板识别"入口 + 识别摘要面板。

**实测**：用真实《投标文件格式.doc》验证，正确识别出 黑体二号标题、宋体五号正文、1.2 倍行距、表格边框 0.5pt、页边距等。

### 5-6. 缺陷修复

| 问题 | 根因 | 修复 |
|---|---|---|
| 知识库文件夹重命名点击无反应 | Electron 不支持 `window.prompt` | 改为 `AppDialog` 输入弹窗 |
| 技术方案"重置"按钮无反应 | Electron 不支持 `window.confirm` | 改为 `AppDialog` 确认弹窗 |

---

## 三、工程基建

- 📄 新增 `CLAUDE.md`（AI 协作指引）及三份功能方案文档；
- 🗄️ 数据库 schema 升级至 v25，同步 `sql/workspace_schema.sql`；
- 🔧 CI「镜像同步」workflow 增加仓库守卫（避免 fork 无凭据反复失败）；
- 🔐 gh 登录打通，代码经「分支 → PR → 合并 main」标准流程入库。

---

## 四、遗留问题与风险（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 中 | 模板识别未接 AI 兜底 | 未知字体/字号等无法归类项当前直接置默认值（本期明确不做） |
| 中 | 主进程离线守卫入口不统一 | 各服务内联判断，仅 updateService 用统一助手，功能等价，属重构建议 |
| 低 | 纵深防御缺口 | `updateService` 下载、`donationService` 主进程侧无离线守卫（当前仅用户主动触发） |
| 低 | `listImageItems` 无专属单测 | 现有测试覆盖检索钳制与图条目规则 |
| ✅ 已解决 | 工作区垃圾文件 | 已通过 `.gitignore` 忽略（`_test_electron*.cjs`、`.tmp-opencode-download/`、`vendor/opencode/`），不再污染 git status |

---

## 五、今日进展（日报）

### ✅ 今日完成

1. **模板 Word 识别**：补全表格/图片样式解析，真实 `.doc` 复测通过；修复行距识别规则 bug。
2. **缺陷修复**：知识库文件夹重命名、技术方案重置按钮（Electron 下 `prompt/confirm` 失效）。
3. **文档产出**：`CLAUDE.md` + 三份功能方案文档 + 开发交接文档。
4. **工程收尾**：gh 登录、CI 镜像同步守卫、代码推送（PR #1 已合并 `main`）、本地 main 同步。
5. **配图复用增强**：优先复用模式（类型无关召回 + 排序兜底）、复用结果弹窗、重新生成确认、`仅重新配图` 入口开放；垃圾文件 `.gitignore` 处理；随 `main` 推送到远程（分支 `feat/illustration-reuse` 合并进 `main`）。

### 📊 关键产出

- 4 个新能力 + 2 项修复，全部验证通过并合并 `main`；
- 数据库 schema 升级至 **v25**。

### 🔜 明日计划

- 在 UI 端到端验收四个新功能（重点：模板识别用真实 `.doc`、配图复用全链路）；
- （可选）模板识别增强：openxmlhelper 作为第二解析源、`.doc/.wps` 自动转 docx。

### ⚠️ 风险与求助

- 遗留问题见第四节（均为低/中优先级，不阻塞）；
- 建议优先验收「配图复用」「模板识别」两个核心价值最高的能力。
