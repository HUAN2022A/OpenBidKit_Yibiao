# 易标投标工具箱 · 开发进展（2026-09-08）

> **项目**：OpenBidKit_Yibiao（开源 AI 标书编写桌面工具，Electron）
> **仓库**：HUAN2022A/OpenBidKit_Yibiao
> **当日 git 结果**：6 个 commit 进 `main`，另 1 个 UI 探索保留在独立分支

---

## 一、总体结论

09-08 重点交付两项能力——**AI 评标**（模拟评标打分并出具评标报告）与**配图复用增强**（优先复用历史图 + 复用结果可追溯），并打磨打赏/导出/模板识别三处体验；同时完成一次从「换色」到「品牌重塑」的 UI 探索（独立分支，未合并）。当日数据库 schema 升级至 **v26**。

| 关键维度 | 结果 |
|---|---|
| 🎯 核心交付 | AI 评标、配图复用增强 |
| 🎨 体验打磨 | 打赏弹窗可关闭、导出格式增强、模板 Word 识别护栏 |
| 🔀 UI 探索 | 铸标/ForgeBid 品牌重塑（`feat/ui-redesign` 分支，未合并） |
| 🗄️ 数据库 | schema v25 → **v26**（AI 评标） |
| 📦 当日改动 | 6 个 commit 进 `main` |

---

## 二、当日交付清单

| # | 能力 / 改动 | 说明 | 状态 |
|---|---|---|---|
| 1 | **AI 评标** | 从技术方案导入正文，按评分标准模拟评标打分并出具评标报告 | ✅ 进 `main` |
| 2 | **配图复用增强** | 优先复用历史图（类型无关召回 + 排序兜底）+ 复用结果弹窗 + 重新生成确认 | ✅ 进 `main` |
| 3 | 打赏弹窗可关闭 | 「不再提醒」按钮 + 全局开关，`promptDismissed` 持久化 | ✅ 进 `main` |
| 4 | 导出格式增强 | 页眉 logo/右栏/下划线、边距行距默认值、新增「正式投标格式」预设 | ✅ 进 `main` |
| 5 | 模板 Word 识别护栏 | 忽略 Word/WPS 出厂纸张、边距、字号，避免覆盖中文标书默认值 | ✅ 进 `main` |
| 6 | UI 品牌重塑 | 易标 → 铸标/ForgeBid，新 logo + 拟态风格 + 流程向导式布局 | 🔀 独立分支未合并 |

---

## 三、分项进展

### 1. AI 评标（模拟评标打分并出具评标报告）

**问题**：缺少对标书质量的自评能力，用户无法在提交前快速知道技术方案大致能拿多少分、扣分点在哪。

**方案**：单评委模拟评标——从已生成的技术方案导入正文与评分标准（招标解析的 `techRequirements`），按评分项逐项打分，产出总分、得分率、各评分项得分、扣分理由、改进建议与总评。`runOptions` 预留 `judgeCount`（当前恒为 1），未来可扩展多评委取均值而不改表结构。

**交付**：`evaluationStore.cjs`（674 行）、`evaluationTask.cjs`（512 行）、`evaluationIpc.cjs`、`EvaluationPage.tsx`（840 行）等 15 文件，+2762 行；数据库 migration **v26**（`evaluation_meta` / `evaluation_documents` / `evaluation_tasks`）；菜单入口 `ai-evaluation` + 埋点路由；实现契约见 `AI评标实现方案.md`。

### 2. 配图复用增强（优先复用 + 结果可追溯）

**问题**：初版配图复用按「类型收窄」召回，HTML 类配图（甘特图/组织架构图等）与知识库图片的固定类型词汇对不上，导致 HTML 配图几乎从不复用历史图，实际仍全部走 AI/HTML 重新生成。

**方案与交付**：
- 新增「优先复用知识库图片」开关（`preferKnowledgeImageReuse`，默认开启）：召回从「按类型收窄」升级为「类型无关关键词召回 + 排序兜底」——零分候选也进入 L2 语义匹配，保证每个配图位置都有复用机会；
- 复用命中回填来源标题/类型，新增「查看配图结果」弹窗，逐项展示复用/新生成/失败状态、把握度与来源；
- 全量重新生成前新增覆盖确认弹窗；「仅重新配图」从开发者面板移入普通用户入口；
- 新增 `illustrationReuseService.test.cjs`（跨词汇匹配、零重叠兜底、相关性排序等用例）。

### 3. 打赏弹窗可关闭

`donation:dismiss-prompts` 通道 + `DonationPromptProvider`「不再提醒」按钮；`DONATION_PROMPT_ENABLED` 全局开关默认关闭，`promptDismissed` 持久化。

### 4. 导出格式增强

页眉新增公司 logo（内置 `header-logo.jpg`）、右栏（`header_right_text`，支持 `{project_name}` 占位）、下划线；导出默认值向真实标书靠拢（边距 2.2cm、行距 1.5、表框黑、页码 `{page}`）；新增「正式投标格式」预设。

### 5. 模板 Word 识别护栏

`templateWordAnalyzer.cjs` 新增出厂默认值护栏：忽略 Word/WPS 出厂纸张（Letter/Legal）、出厂边距（1/1.25 英寸）、出厂字号（五号及更小），避免误覆盖中文标书默认值。

### 6. UI 品牌重塑（独立分支，未合并）

一次从「换色」到「品牌重塑 + 布局重构」的完整探索，落定在 `feat/ui-redesign` 分支（commit `e850cd7`），**未合并进 main**：

| 维度 | 内容 |
|---|---|
| 改名 | 易标 → 铸标 / ForgeBid（窗口标题、preload `appName`、打包 `productName`） |
| 新 logo | 深灰黑圆角方块 + 工业红菱形 + 白色向上箭头，7 档 PNG + `icon.ico` / `icon.icns` |
| 拟态风格 | 浅灰基底 + 柔和双阴影（凸起/凹陷）、圆角收窄、工业红强调、深色侧边栏 |
| 布局 | 信息架构从「功能域分组」改为「流程阶段分组」：工作台首页 + 准备/生成/审核/导出 + 顶栏 |

对应 open [PR #3](https://github.com/HUAN2022A/OpenBidKit_Yibiao/pull/3)，暂不合并。

---

## 四、工程基建

- 🗄️ 数据库 schema 升级至 **v26**，同步 `sql/workspace_schema.sql`；
- 🧹 `.gitignore` 新增 `client/.tmp-opencode-download/`、`client/vendor/opencode/`、`client/_test_electron*.cjs`，清理工作区垃圾文件噪音；
- 🔧 移除 AI 评标菜单的「正在开发中」占位提示；
- 📄 新增 `AI评标实现方案.md`、`开发交接-2026-09-08.md` 两份文档。

---

## 五、遗留问题与风险（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | UI 分支取舍 | `feat/ui-redesign`（铸标）与旧 UI「易标」二选一：合并 PR #3 或删除分支，需明确决策 |
| 中 | 顶栏搜索框未接逻辑 | 跨功能检索模板/素材/知识库需要新的 IPC 通道 |
| 低 | `listImageItems` 无专属单测 | 现有测试覆盖检索钳制与图条目规则 |
| 低 | 模板识别未接 AI 兜底 | 未知字体/字号等无法归类项当前直接置默认值 |
| 低 | 主进程离线守卫入口不统一 | 各服务内联判断，功能等价，属重构建议 |

---

## 六、次日计划

- 端到端验收 **AI 评标** 与 **配图复用增强**（重点：真实技术方案导入打分、配图优先复用全链路、复用结果弹窗）；
- 明确 UI 品牌重塑的取舍：合并 PR #3 继续打磨，或删除分支维持旧 UI；
- 视验收结果决定是否补 `listImageItems` 单测、顶栏搜索逻辑。
